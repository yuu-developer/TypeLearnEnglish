<h1>結果</h1>

<div class="result-container">
  <div class="result-item">
    <p><strong>スコア:</strong> <%= @practice.score %></p>
  </div>
  <div class="result-item">
    <p><strong>難易度:</strong> <%= @practice.difficulty_level %></p>
  </div>
  <!-- <div class="result-item">
    <p><strong>Start Time:</strong> <%= @practice.start_time %></p>
  </div>
  <div class="result-item">
    <p><strong>End Time:</strong> <%= @practice.end_time %></p>
  </div> -->

  <div class="button-container">
    <%= link_to 'もう一度', new_practice_path, class: 'btn btn-primary' %>
    <%= link_to 'ホームへ', root_path, class: 'btn btn-secondary' %>
  </div>
</div>

<style>
.result-container {
  background-color: #f8f9fa;
  border-radius: 8px;
  padding: 20px;
  margin: 20px auto;
  max-width: 600px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

.result-item {
  margin: 10px 0;
  font-size: 1.2rem;
}

.result-item p {
  margin: 0;
  padding: 10px;
  background: #121010;
  border-radius: 4px;
  border: 1px solid #16181a;
}

.button-container {
  display: flex;
  justify-content: space-between;
  margin-top: 20px;
}

.btn-primary {
  background-color: #007bff;
  border-color: #007bff;
}

.btn-primary:hover {
  background-color: #0056b3;
  border-color: #0056b3;
}

.btn-secondary {
  background-color: #6c757d;
  border-color: #6c757d;
}

.btn-secondary:hover {
  background-color: #565e64;
  border-color: #565e64;
}
</style>